#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo '✓ Commit Successfull. Pushing the changes to remote'

git push -u origin ||
(
    echo '❌ Push Failed. Please pull or rebase from origin and then try committing again!';
    false;
)